const { User } = require('./User');
const Article = require('./Article');
const Payment = require('./Payment');
const Subscription = require('./Subscription');

module.exports = { User, Article, Payment, Subscription };
